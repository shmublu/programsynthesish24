This is an html page that creates boolean or integer SyGuS queries and transforms the solutions to those queries into valid JS code.
To use, run "npm start" or "node start". Follow the on screen prompts in the first two text boxes to enter the parameters to your query. If it is solvable by cvc4, then it will return the solution.



dependencies:
cvc4

